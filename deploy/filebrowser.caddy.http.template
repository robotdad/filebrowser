http://FILEBROWSER_FQDN:FILEBROWSER_CADDY_PORT {
    forward_auth localhost:FRONTDOOR_PORT {
        uri /api/auth/validate
        copy_headers X-Authenticated-User
    }

    reverse_proxy localhost:FILEBROWSER_PORT
}
